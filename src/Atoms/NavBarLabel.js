import React, { Component } from 'react';

class NavBarLabel extends Component {
    render() {
        return (
            <div>
                {/* A label for each Atom in the NavBar. Props is the label input for each atom.*/}
            </div>
        );
    }
}

export default NavBarLabel;