import React, { Component } from 'react';

class TotalPoints extends Component {
    render() {
        return (
            <div>
                {/* Takes in an array prop, adds the array, generates a total
                    for the user, and returns a div with the output. */}
            </div>
        );
    }
}

export default TotalPoints;