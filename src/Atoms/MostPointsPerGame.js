import React, { Component } from 'react';

class MostPointsPerGame extends Component {
    render() {
        return (
            <div>
                {/* Takes the last item in the array and compares them to find the most points in that round.*/}
            </div>
        );
    }
}

export default MostPointsPerGame;